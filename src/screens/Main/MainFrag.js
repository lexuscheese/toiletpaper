import React from 'react';
import {Text,
} from 'react-native';

export default class mainFrag extends Component<{}> {
    render() {
        return (
            <Text>
                MainFrag
            </Text>
        );
    }
};